# Phase 4 — Downstream Gate  (PASS)

Validate the closed 7-axiom set against the live AI-agent projection (`~/.claude/guidelines/axiom-principles/`). Orphan lore → missing axiom. Orphan axiom → doesn't project. Either → loop back to Phase 3.

The projection is the **AI-agent substrate** of the canon (canon scope = any finite actor; this projection = the AI one). So substrate-specific lore content correctly has *no* canon axiom — that's the layering (§ spine 4), not a gap.

---

## Up — every lore file traces to an axiom (or is correctly substrate-only)

| Lore file | Traces to |
|---|---|
| attention-is-north-star | **Apex** (attention) + SoC + Atomic + Outcome (NEVER DRIFT = anchor / anti-drift) |
| attention-budget | **Apex** — Finite-fuel property (clean budget after baseline) |
| single-source-of-truth | **SSOT** (one truth + clear pillars = SSOT + Boundary) |
| pipeline-design | **SSOT** + Volatile property (state=file presence, cold-start) + Irreversible property (verify-before-cleanup = own the trace) |
| prompt-anchoring | **Boundary** (boundaries-are-routes = routing half) + *substrate-only*: role-names-prime-posture (LLM mechanic) |
| escalation-design | **Boundary** (route-up / surface) + **Outcome** (human owns the anchor → human-authority split) |
| harness-md-discipline | **Outcome** (cut test) + Boundary (positive≥negative) + Grounding (verification, pre-flight) + Apex (bloat=budget) |
| harness-audit-signals | SoC + Atomic (overload = atomic breach) + Apex (always-on bloat) |
| layer-placement | Boundary (placement = correctness boundary) + Apex (ambient cost) + *substrate-only*: layer table |
| knowledge-placement | SSOT (one home per fact) + Boundary (scope) + *substrate-only*: CLAUDE.md/MEMORY surfaces |
| skill-authoring | Boundary (IO contract) + Grounding (return questions, not guesses) + *substrate-only*: doer/thinker |
| candidate-not-artifact | Boundary (router vs gate = two surfaces) + Grounding (gate judges substance) |
| investigation-value-decomposition | Grounding (investigate the real before acting) + Apex (audit scales with centrality) |
| agent-shapes | *substrate-only* (skill/subagent/gateway) — derives from Apex + SoC + Atomic + Boundary + Leverage |

**No orphan lore.** Every file traces up, or is substrate-only detail the canon correctly delegates to the projection.

## Down — every axiom projects into the lore

| Axiom | Projects to |
|---|---|
| Outcome-Driven | cut-test, NEVER-DRIFT anchor, human-owns-goal |
| Grounding | verification discipline, investigation-value, return-questions |
| SoC | "SoC = attention boundary distribution", audit-signals |
| Atomic | "one container, one goal", agent-shapes |
| Boundary | boundaries-are-routes, escalation, layer/knowledge placement, candidate gate |
| Leverage | dispatch to subagents/tools/skills (agent-shapes), outward motto |
| SSOT | single-source-of-truth, knowledge-placement, pipeline-design |

**No orphan axiom.** All 7 project down.

---

## Forces considered for a missing axiom — none needed

- **role-names-prime-posture** (prompt-anchoring) — substrate-specific cognition mechanic; its load-bearing half ("authority rides routes, knowledge rides context, not labels") = Boundary + Grounding. Projection-only.
- **fresh-context audit** (investigation-value) — independent unbiased vantage = Grounding (verify the real) + Atomic (clean container). Covered.
- **three-roles per artifact** (pipeline-design) — Boundary (ownership of each transition) + trace-handling (cleaner). Covered.

## Result

**PASS — no mismatch, no loop-back.** Bonus validation: the projection's own apex (`attention-is-north-star`) is *literally* the canon apex ("attention is all you need"), narrowed to the AI substrate. Canon and projection align at the crown.

→ Phase 5 (content: write the 7 axioms, frame-neutral) + Phase 6 (SEED amendments).
